---
allowed-tools: Bash
description: Reset to previous commit with options (soft/mixed/hard)
argument-hint: [soft|mixed|hard] (default: soft)
---

# 직전 커밋으로 돌아가기

마지막 커밋을 취소하고 이전 상태로 돌아갑니다.

## 사용법

```bash
/git:back              # 기본값: --soft (변경사항 staging 유지)
/git:back soft         # --soft: 변경사항 staged로 유지
/git:back mixed        # --mixed: 변경사항 working directory에 유지
/git:back hard         # --hard: 변경사항 완전 삭제 (위험!)
```

## 옵션 설명

| 옵션 | 동작 | 사용 시기 |
|------|------|---------|
| **soft** (기본) | 커밋 취소, 변경사항은 staging area 유지 | 커밋 메시지 수정, 파일 구성 변경 |
| **mixed** | 커밋 취소, 변경사항은 working directory 유지 | 일부 파일만 재커밋, 코드 재정리 |
| **hard** | 커밋 취소, 모든 변경사항 삭제 | 잘못된 커밋 완전 폐기 (되돌릴 수 없음!) |

## 실행 프로세스

### 1단계: 현재 상태 확인
```bash
git log --oneline -2
git status
```

직전 커밋 정보와 현재 working directory 상태 표시

### 2단계: 사용자 확인
- 직전 커밋 해시, 메시지 표시
- 선택한 reset 옵션 재확인
- 경고: hard 옵션은 되돌릴 수 없음

### 3단계: git reset 실행
```bash
git reset --soft HEAD~1    # soft 모드
git reset --mixed HEAD~1   # mixed 모드 (기본값)
git reset --hard HEAD~1    # hard 모드 (위험!)
```

### 4단계: 결과 확인
```bash
git log --oneline -2
git status
```

리셋 후 새로운 HEAD 상태 표시

## 출력 예시

```
## ⏮️ 직전 커밋으로 리셋

### 📋 현재 상태
- 현재 HEAD: abc1234 "Fix: 버그 수정"
- 이전 상태: def5678 "Feat: 새 기능"

### 🔧 리셋 옵션: soft
- 변경사항 유지: ✅ (staging area)
- 커밋 취소: ✅

### ✅ 리셋 완료
- 새 HEAD: def5678 "Feat: 새 기능"
- 변경사항: 2 파일 (staged)

### 📝 다음 단계
```bash
git add .          # 파일 다시 스테이징 (필요 시)
git commit -m "..." # 새 커밋 생성
```
```

## 주의사항

⚠️ **soft/mixed**: 안전함. 언제든지 원래 상태로 복구 가능
❌ **hard**: 변경사항이 **완전히 삭제**됨. 매우 조심!

```bash
# hard 사용 후 복구하려면 reflog 확인
git reflog
git reset --hard abc1234  # 이전 상태로 복구
```
